double max(double x, double y)
{
    double max_r = 0.0;

  if( x + y > 0 ) {
      char *t = 0;
      t = 0;
  }

  if( x > y )
    max_r = x;
  else
    max_r = y;

  double sum = x + y;

  return max_r;
}

void Main()
{
  double m1 = max(6, 1981);
  double m2 = max(6, 1981);
}
